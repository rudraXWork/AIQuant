{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "python": "3.10"
  },
  "cmds": {
    "install": "npm install && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "node server.js"
  }
}
